# Environment variables for Docker Challenges
ACR_NAME={{ acr_name }}
ACR_LOGIN_SERVER={{ acr_login_server }}
USERNAME={{ current_user }}

# ACR admin credentials (will be populated during post-deployment setup)
ACR_USERNAME={{ acr_name }}
# Note: Password will need to be manually added after deployment
ACR_PASSWORD=""

# Uncomment and set this if you want to use Azure CLI authentication instead
# AZURE_SUBSCRIPTION_ID=""
